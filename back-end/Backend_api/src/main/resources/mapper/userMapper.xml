<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.ass.dao.PortFolioMapper">

	<select id="searchAllUser" resultType="portfolio">
		SELECT * FROM portfolio;
	</select>

	<select id="searchOnceUser" parameterType="String" resultType="portfolio">
		SELECT * FROM portfolio WHERE email=#{email};
	</select>

	<delete id="deleteUser" parameterType="String">
		DELETE FROM portfolio
		WHERE email=#{email};
	</delete>

	<update id="updateUser" parameterType="user">
		UPDATE portfolio set
		authority=#{authority} WHERE email=#{email};
	</update>

	<insert id="insertUser" parameterType="user">
		INSERT INTO portfolio values (#{email}, default, #{authority}, 0);
	</insert>
	<select id="countUserAdmin" resultType="int">
		SELECT count(*) FROM user WHERE authority ='admin'
	</select>
</mapper>